{
  "name": "session-hijacking-lab",
  "version": "0.1.0",
  "private": true,
  "description": "Local classroom lab for demonstrating vulnerable session cookies.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "start:fixed": "set SESSION_MODE=fixed&& set SESSION_COOKIE_SECURE=false&& node src/server.js",
    "dev": "node --watch src/server.js",
    "dev:fixed": "set SESSION_MODE=fixed&& set SESSION_COOKIE_SECURE=false&& node --watch src/server.js",
    "test": "vitest --run"
  },
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^5.1.0",
    "express-session": "^1.19.0"
  },
  "devDependencies": {
    "supertest": "^7.1.1",
    "vitest": "^3.2.4"
  }
}
