---
phase: "02-session-reuse-attack"
plan: "02-02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - tests/session-reuse-attack.test.js
  - docs/session-reuse-attack.md
autonomous: true
requirements:
  - VULN-02
  - ATK-01
  - ATK-02
requirements_addressed:
  - VULN-02
  - ATK-01
  - ATK-02
user_setup: []
must_haves:
  truths:
    - "D-03: The replay proof shows /dashboard denial before adding the copied sid cookie."
    - "D-04: The replay proof sends only the copied sid cookie name-value pair."
    - "D-07: A second local client reaches /dashboard and sees fake private user data without login."
    - "D-08: The walkthrough records concise evidence points for later slides without creating final slides."
    - "D-09: A focused Vitest/Supertest replay proof test logs in, extracts sid, replays it from a separate request, and asserts dashboard data."
    - "D-10: Evidence and tests remain local-only and fake-data-only."
    - "D-11: Evidence guidance includes reset and cleanup."
    - "D-12: The explanation identifies cookie possession as sufficient for impersonation in vulnerable mode."
    - "D-15: No fixed-mode session behavior is implemented in this plan."
  artifacts:
    - path: "tests/session-reuse-attack.test.js"
      provides: "Automated proof that a copied sid cookie can be replayed from a separate request"
      contains: "Cookie"
    - path: "docs/session-reuse-attack.md"
      provides: "Evidence checklist for attack rehearsal and future slides"
      contains: "npm test"
  key_links:
    - from: "tests/session-reuse-attack.test.js"
      to: "src/server.js"
      via: "imports createApp for local Supertest replay proof"
      pattern: "createApp"
    - from: "tests/session-reuse-attack.test.js"
      to: "/dashboard"
      via: "separate request with copied sid cookie"
      pattern: "dashboard"
---

# Plan 02-02: Registrar evidencias da exploracao e prova automatizada

<objective>
As a demo presenter, I want automated and manual evidence that the copied `sid` cookie grants dashboard access in a second local client so that the team can prove the vulnerable behavior during rehearsal and later reuse the evidence in slides.

This plan covers VULN-02, ATK-01, and ATK-02 by adding a focused replay proof test and strengthening the evidence checklist in the Phase 2 walkthrough created by Plan 02-01.
</objective>

<context>
- `.planning/PROJECT.md`
- `.planning/REQUIREMENTS.md`
- `.planning/ROADMAP.md`
- `.planning/phases/02-session-reuse-attack/02-CONTEXT.md`
- `.planning/phases/02-session-reuse-attack/02-RESEARCH.md`
- `.planning/phases/02-session-reuse-attack/02-VALIDATION.md`
- `.planning/phases/02-session-reuse-attack/02-PATTERNS.md`
- `docs/session-reuse-attack.md`
- `tests/session-cookie.test.js`
- `src/server.js`
- `src/session/vulnerable-session.js`
- `src/middleware/require-auth.js`
- `src/data/users.js`
- `src/views/dashboard.ejs`
</context>

<must_haves>
- VULN-02: A copied vulnerable `sid` cookie must be shown reusable.
- ATK-01: Evidence must support the controlled cookie-copy/replay route.
- ATK-02: A second client must access protected dashboard data without password entry.
- D-03: The second client must be denied before replay.
- D-04: The proof uses only the `sid` cookie value/name-value pair.
- D-07: The proof of compromise is `/dashboard` access with fake private user data in the second client.
- D-08: Evidence should be concise and useful for future slides, without creating final slides now.
- D-09: Add one focused automated replay proof test using Vitest/Supertest.
- D-10: Keep all proof local and fake-data-only.
- D-11: Include reset/cleanup evidence guidance.
- D-12: Explain the violated principle in plain terms.
- D-15: Do not implement fixed-mode behavior in this phase.
</must_haves>

<threat_model>
## Threats

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-02-01 | Automated test could normalize offensive behavior without local-only framing. | high | Test names and docs describe a local vulnerable lab with fake users; docs retain ethical warning. |
| T-02-02 | Test could accidentally prove normal persistent-agent auth instead of cookie replay. | medium | Use a separate request/client for replay and assert denial before setting the copied cookie. |
| T-02-03 | Evidence checklist could collect sensitive or real data. | high | Checklist must explicitly capture only fake dashboard data from `alice` or `bruno` on localhost. |

## Residual Risk

No unmitigated high-severity threat remains if the test and documentation keep local-only wording and fake-data assertions.
</threat_model>

## Artifacts this phase produces

- New file path: `tests/session-reuse-attack.test.js` or an equivalent focused test addition in `tests/session-cookie.test.js`
- Test behavior: copied `sid` cookie replay in a separate local client
- Documentation section: evidence checklist for attack rehearsal and future slides

<tasks>
  <task id="02-02-01" type="test">
    <name>Add a focused session replay proof test</name>
    <read_first>
      - `tests/session-cookie.test.js`
      - `src/server.js`
      - `src/session/vulnerable-session.js`
      - `src/middleware/require-auth.js`
      - `src/data/users.js`
      - `src/views/dashboard.ejs`
      - `.planning/phases/02-session-reuse-attack/02-CONTEXT.md`
      - `.planning/phases/02-session-reuse-attack/02-RESEARCH.md`
    </read_first>
    <action>
      Add `tests/session-reuse-attack.test.js` unless extending `tests/session-cookie.test.js` is clearly cleaner. The test must create a fresh `app` with `createApp()`, prove `GET /dashboard` without a cookie returns `302` and `Location: /login`, log in as `alice` with form credentials, extract the `sid` cookie from `Set-Cookie`, derive a replay cookie name-value pair such as `sid=...`, send that cookie on a separate `request(app).get("/dashboard")` call, and assert `200` plus `Painel protegido`, `Alice Demo`, and `Relatorio interno ficticio`.
    </action>
    <verify>
      Run `npm test`.
    </verify>
    <acceptance_criteria>
      - A replay proof test exists in `tests/session-reuse-attack.test.js` or `tests/session-cookie.test.js`.
      - The test includes a denial-before-replay assertion for `/dashboard`.
      - The replay request does not use the original `request.agent(app)` authenticated session.
      - The replay request sends the copied `sid` cookie and receives `200`.
      - The response assertions include fake private dashboard data for Alice.
      - `npm test` exits 0.
    </acceptance_criteria>
  </task>

  <task id="02-02-02" type="docs">
    <name>Strengthen the evidence checklist in the walkthrough</name>
    <read_first>
      - `docs/session-reuse-attack.md`
      - `.planning/phases/02-session-reuse-attack/02-VALIDATION.md`
      - `tests/session-reuse-attack.test.js`
      - `tests/session-cookie.test.js`
    </read_first>
    <action>
      Update `docs/session-reuse-attack.md` with an evidence checklist that captures exactly four proof points: victim logged in locally, `sid` copied from DevTools, second client denied before replay, and second client accessing `/dashboard` without password after replay. Include `npm test` as automated evidence and state that screenshots/notes must contain only fake local dashboard data.
    </action>
    <verify>
      Run `Select-String -Path docs/session-reuse-attack.md -Pattern "Checklist","sid","sem senha","npm test","fake"` and confirm all patterns are present.
    </verify>
    <acceptance_criteria>
      - The walkthrough includes the four evidence points listed in the action.
      - The walkthrough mentions `npm test` as automated replay evidence.
      - The evidence instructions explicitly forbid real credentials, real domains, and external targets.
      - The walkthrough still defers mitigation/fixed-mode proof to later phases.
    </acceptance_criteria>
  </task>

  <task id="02-02-03" type="verify">
    <name>Run final Phase 2 verification</name>
    <read_first>
      - `.planning/phases/02-session-reuse-attack/02-VALIDATION.md`
      - `docs/session-reuse-attack.md`
      - `README.md`
      - `tests/session-reuse-attack.test.js`
      - `tests/session-cookie.test.js`
    </read_first>
    <action>
      Run the full test suite and documentation checks for the Phase 2 acceptance criteria. Confirm that docs and tests together prove: cookie copied locally, second client denied before replay, second client accesses dashboard without password after replay, and no external targets or real data are used.
    </action>
    <verify>
      Run `npm test`. Run `Select-String -Path docs/session-reuse-attack.md,README.md -Pattern "127.0.0.1","sid","sem senha","docs/session-reuse-attack.md"`.
    </verify>
    <acceptance_criteria>
      - `npm test` exits 0.
      - Documentation checks find local host, `sid`, no-password access wording, and README link.
      - The final diff does not modify `src/session/vulnerable-session.js` to add fixed-mode behavior.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- `npm test`
- `Select-String -Path docs/session-reuse-attack.md -Pattern "Checklist","sid","sem senha","npm test","fake"`
- `Select-String -Path docs/session-reuse-attack.md,README.md -Pattern "127.0.0.1","sid","sem senha","docs/session-reuse-attack.md"`
</verification>

<success_criteria>
- A focused automated test proves cookie replay from a separate local client.
- The test shows denial before replay and dashboard access after replay.
- Evidence checklist is ready for rehearsal and future slides.
- The phase remains local-only, fake-data-only, and does not implement mitigations.
</success_criteria>
