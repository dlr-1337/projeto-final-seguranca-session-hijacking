---
phase: "03"
phase_name: session-security-fixes
plan: "03-02"
title: "Invalidate logout, clear cookies, and document corrected behavior"
type: tdd
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/server.js
  - src/views/dashboard.ejs
  - src/session/session-mode.js
  - tests/session-logout.test.js
  - docs/session-security-fixes.md
  - README.md
autonomous: true
requirements:
  - FIX-01
  - FIX-02
requirements_addressed:
  - FIX-01
  - FIX-02
user_setup: []
must_haves:
  truths:
    - "D-09: Fixed mode may regenerate the session after successful login if it remains simple to explain."
    - "D-10: Logout destroys the server-side session, clears the correct cookie, and redirects safely."
    - "D-11: Prefer POST /logout from a dashboard form over state-changing GET."
    - "D-12: Do not add a deliberately broken logout just to create another vulnerability."
    - "D-13: README documents Phase 3 startup and fixed cookie inspection."
    - "D-14: docs/session-security-fixes.md documents corrected cookies, logout, expiration, and OWASP/Secure SDLC mapping."
    - "D-15: Focused tests cover logout invalidation while existing vulnerable tests remain meaningful."
    - "D-16: Full copied-cookie replay failure proof stays in Phase 4."
  artifacts:
    - path: "tests/session-logout.test.js"
      provides: "Automated proof that logout invalidates server session state and clears the cookie"
      contains: "POST /logout"
    - path: "docs/session-security-fixes.md"
      provides: "Phase 3 corrected behavior explanation for rehearsal and later slides"
      contains: "OWASP"
---

# Plan 03-02 - Add Real Logout And Corrected Behavior Docs

<objective>
As a demo operator, I want logout to invalidate the server-side session and remove the browser cookie so that the corrected mode demonstrates session lifecycle control, not only safer cookie attributes.

Purpose: Use TDD to prove logout destroys server-side state and then document the corrected behavior for the classroom explanation.
Output: `POST /logout`, dashboard logout form, logout invalidation tests, Phase 3 fix documentation, and README links.
</objective>

<context>
- `.planning/PROJECT.md`
- `.planning/REQUIREMENTS.md`
- `.planning/ROADMAP.md`
- `.planning/STATE.md`
- `.planning/phases/03-session-security-fixes/03-CONTEXT.md`
- `.planning/phases/03-session-security-fixes/03-RESEARCH.md`
- `.planning/phases/03-session-security-fixes/03-01-PLAN.md`
- `AGENTS.md`
- `README.md`
- `docs/session-reuse-attack.md`
- `src/server.js`
- `src/session/fixed-session.js`
- `src/session/session-mode.js`
- `src/middleware/require-auth.js`
- `src/views/dashboard.ejs`
- `tests/session-fixed-cookie.test.js`
- `tests/session-reuse-attack.test.js`
</context>

<threat_model>

| ID | Threat | Severity | Mitigation In This Plan |
|----|--------|----------|-------------------------|
| T-03-05 | Logout clears only the browser cookie and leaves the server session reusable. | high | Test that the old cookie cannot access `/dashboard` after `POST /logout`. |
| T-03-06 | A state-changing GET logout normalizes unsafe HTTP semantics. | medium | Use `POST /logout` from a dashboard form. |
| T-03-07 | Documentation claims the full mitigation proof before Phase 4 verifies replay failure. | medium | Docs state Phase 3 implements fixes and Phase 4 proves the same attack fails. |
| T-03-08 | Logout changes accidentally remove the vulnerable-mode replay evidence. | medium | Keep existing Phase 2 tests passing and avoid changing the vulnerable cookie contract. |

</threat_model>

<must_haves>
- FIX-01: Corrected cookie behavior remains documented and test-backed.
- FIX-02: Logout invalidates the server-side session and removes the client cookie.
- D-09: Regenerate session after fixed-mode login if it remains simple.
- D-10: Logout destroys server-side state, clears the right cookie, and redirects safely.
- D-11: Use `POST /logout` from a dashboard form.
- D-12: Do not add a deliberately broken logout.
- D-13: README includes Phase 3 fixed-mode guidance.
- D-14: `docs/session-security-fixes.md` explains corrected cookies, logout, expiration, OWASP, and Secure SDLC.
- D-15: Add focused logout tests.
- D-16: Leave full replay-fails proof to Phase 4.
</must_haves>

<tasks>

<task id="03-02-T1" type="tdd:red">
  <title>Write failing logout invalidation tests</title>
  <read_first>
    - `.planning/phases/03-session-security-fixes/03-CONTEXT.md`
    - `.planning/phases/03-session-security-fixes/03-RESEARCH.md`
    - `.planning/phases/03-session-security-fixes/03-01-PLAN.md`
    - `tests/session-fixed-cookie.test.js`
    - `src/server.js`
    - `src/session/session-mode.js`
    - `src/views/dashboard.ejs`
  </read_first>
  <files>
    - `tests/session-logout.test.js`
  </files>
  <action>
    Add `tests/session-logout.test.js`. The test should create the app in fixed mode, log in as `alice`, capture the active session cookie, verify `/dashboard` is accessible before logout, call `POST /logout` with that cookie, assert a redirect to `/login`, assert `Set-Cookie` clears the active cookie, and then assert `/dashboard` with the old cookie redirects to `/login` or is denied. Add a second narrow assertion if useful that the dashboard renders a logout form posting to `/logout`.
  </action>
  <verify>
    `npm test` exits non-zero before the logout route/form exists, with failures caused by missing logout behavior.
  </verify>
  <acceptance_criteria>
    - `tests/session-logout.test.js` exercises `POST /logout`.
    - The test checks server-side invalidation by retrying `/dashboard` with the old cookie after logout.
    - The test checks the client cookie is cleared.
    - The RED run fails because logout is not implemented yet.
  </acceptance_criteria>
</task>

<task id="03-02-T2" type="tdd:green">
  <title>Implement logout route, cookie clearing, and optional fixed login regeneration</title>
  <read_first>
    - `tests/session-logout.test.js`
    - `tests/session-fixed-cookie.test.js`
    - `src/server.js`
    - `src/session/session-mode.js`
    - `src/views/dashboard.ejs`
    - `src/middleware/require-auth.js`
  </read_first>
  <files>
    - `src/server.js`
    - `src/session/session-mode.js`
    - `src/views/dashboard.ejs`
  </files>
  <action>
    Add `POST /logout` to `src/server.js`. The route should use the active session mode metadata to clear the correct cookie name/path, call `req.session.destroy`, and redirect to `/login`. Add a compact logout form to `src/views/dashboard.ejs` that posts to `/logout`. If the fixed-mode login path can safely regenerate the session without obscuring the teaching flow, wrap `req.session.regenerate` before setting `userId` in fixed mode only. Keep vulnerable mode's cookie settings unchanged.
  </action>
  <verify>
    `npm test` exits 0.
  </verify>
  <acceptance_criteria>
    - `POST /logout` exists and calls `req.session.destroy`.
    - Logout clears the active session cookie using the correct cookie name and Path=/.
    - After logout, the old cookie cannot access `/dashboard`.
    - `src/views/dashboard.ejs` includes a form that posts to `/logout`.
    - Existing Phase 2 replay test still passes in vulnerable mode.
    - `npm test` exits 0.
  </acceptance_criteria>
</task>

<task id="03-02-T3" type="tdd:refactor">
  <title>Document corrected behavior and Phase 4 boundary</title>
  <read_first>
    - `README.md`
    - `docs/session-reuse-attack.md`
    - `.planning/phases/03-session-security-fixes/03-CONTEXT.md`
    - `.planning/phases/03-session-security-fixes/03-RESEARCH.md`
    - `src/session/fixed-session.js`
    - `src/server.js`
  </read_first>
  <files>
    - `docs/session-security-fixes.md`
    - `README.md`
  </files>
  <action>
    Create `docs/session-security-fixes.md` with sections for objetivo, como iniciar modo vulneravel/corrigido, tabela de atributos corrigidos, expiracao, logout/invalidation behavior, Secure cookie limitation on local HTTP, OWASP relationship, Secure SDLC relationship, and what Phase 4 will verify. Update README with a link to this document and a concise Phase 3 summary. Keep final slide copy and full replay-fails proof out of this phase.
  </action>
  <verify>
    Run `npm test`. Run `Select-String -Path docs/session-security-fixes.md,README.md -Pattern "HttpOnly","Secure","SameSite","logout","OWASP","Phase 4"` and confirm all patterns are present.
  </verify>
  <acceptance_criteria>
    - `docs/session-security-fixes.md` exists.
    - The doc explains fixed cookies, expiration, logout invalidation, OWASP, and Secure SDLC.
    - The doc states Phase 4 proves the same replay attack fails.
    - `README.md` links to `docs/session-security-fixes.md`.
    - `npm test` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>

Run:

- `npm test`
- `Select-String -Path docs/session-security-fixes.md,README.md -Pattern "HttpOnly","Secure","SameSite","logout","OWASP","Phase 4"`
- Optional manual check: log in, use the dashboard logout form, then try `/dashboard` again with the previous cookie.

</verification>

<success_criteria>

- FIX-02 is satisfied: logout destroys server-side session state and clears the client cookie.
- FIX-01 remains satisfied and documented after logout changes.
- The dashboard provides a simple logout action.
- The docs explain corrected behavior without stealing Phase 4's replay-failure proof.

</success_criteria>

## Artifacts this phase produces

- `POST /logout` route in `src/server.js`.
- Dashboard logout form in `src/views/dashboard.ejs`.
- Active cookie metadata used for `res.clearCookie`.
- Optional fixed-mode `req.session.regenerate` login hardening.
- `tests/session-logout.test.js`.
- `docs/session-security-fixes.md`.
- README Phase 3 documentation link.

## Source Audit

| Source | ID | Requirement / Decision | Plan | Status |
|--------|----|------------------------|------|--------|
| REQ | FIX-01 | Corrected cookie behavior remains documented and test-backed | 03-01, 03-02 | COVERED |
| REQ | FIX-02 | Logout invalidates server session and removes client cookie | 03-02 | COVERED |
| CONTEXT | D-09 | Optional fixed-mode session regeneration | 03-02 | COVERED |
| CONTEXT | D-10 | Logout destroys session, clears cookie, redirects safely | 03-02 | COVERED |
| CONTEXT | D-11 | Prefer `POST /logout` | 03-02 | COVERED |
| CONTEXT | D-12 | Do not add broken logout | 03-02 | COVERED |
| CONTEXT | D-13 | README documents Phase 3 | 03-01, 03-02 | COVERED |
| CONTEXT | D-14 | `docs/session-security-fixes.md` documents corrections | 03-02 | COVERED |
| CONTEXT | D-15 | Focused tests cover fixed behavior and logout | 03-01, 03-02 | COVERED |
| CONTEXT | D-16 | Replay-fails proof stays in Phase 4 | 03-02 | COVERED |
